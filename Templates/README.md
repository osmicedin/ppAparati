# Predložak

`ZapisnikTemplate.docx` je aplikacijski predložak izveden iz dostavljenog zapisnika. Dinamički tekst je označen Word content-control tagovima, a red `ReportRows` aplikacija zamjenjuje podacima iz baze.
